llm:
  providers:
    openai_direct:
      kind: openai
      endpoint: https://api.openai.com
      auth:
        type: bearer
        secret_ref: env://OPENAI_API_KEY

  models:
    gpt-4o-mini:
      provider: openai_direct

keys:
  acme/alice/sk-test-001:
    workspace: acme
    user: alice
    llm:
      models:
        gpt-4o-mini:
          provider: openai_direct
