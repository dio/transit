llm:
  providers:
    openai:
      kind: openai
      endpoint: https://api.openai.com
      auth:
        type: bearer
        secret_ref: env://TEST_OPENAI_KEY

  models:
    gpt-4o:
      provider: openai

keys:
  acme/alice/sk-test-key-001:
    workspace: acme
    user: alice
    llm:
      models:
        gpt-4o-mini:
          provider: openai
          name: gpt-4o-mini
