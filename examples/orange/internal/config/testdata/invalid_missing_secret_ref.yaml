llm:
  # auth.type=bearer requires secret_ref (schema if/then constraint).
  providers:
    openai:
      kind: openai
      endpoint: https://api.openai.com
      auth:
        type: bearer

  models:
    gpt-4o:
      provider: openai
