// service.proto — SnapshotService: control-plane → data-plane config delivery.
//
// The data-plane proxy (client) opens one Watch stream on startup and keeps it
// alive. The server pushes a new SnapshotEnvelope whenever the config changes,
// and emits a Heartbeat on the idle timer so the client can distinguish "no
// changes" from "stream is stalled."
//
// Fetch is the cold-start / reconnect path. When last_version and last_checksum
// both still match, the server returns Unchanged instead of re-sending the full
// payload; otherwise it returns the current SnapshotEnvelope.
//
// All request messages are annotated with buf.validate constraints. The server
// should call protovalidate.Validate() on every incoming request before
// processing. Generated clients may call it on response messages as an
// extra integrity check.
//
// Field numbers are stable and must not be reused once released.

// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: orange/config/v1/service.proto

package configv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/dio/transit/examples/orange/api/orange/config/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// SnapshotServiceName is the fully-qualified name of the SnapshotService service.
	SnapshotServiceName = "orange.config.v1.SnapshotService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// SnapshotServiceWatchProcedure is the fully-qualified name of the SnapshotService's Watch RPC.
	SnapshotServiceWatchProcedure = "/orange.config.v1.SnapshotService/Watch"
	// SnapshotServiceFetchProcedure is the fully-qualified name of the SnapshotService's Fetch RPC.
	SnapshotServiceFetchProcedure = "/orange.config.v1.SnapshotService/Fetch"
)

// SnapshotServiceClient is a client for the orange.config.v1.SnapshotService service.
type SnapshotServiceClient interface {
	// Watch is the steady-state live-update channel.
	// The client keeps the stream open; the server pushes a new SnapshotEnvelope
	// on every config change and emits a Heartbeat on the idle timer.
	Watch(context.Context, *connect.Request[v1.WatchRequest]) (*connect.ServerStreamForClient[v1.WatchResponse], error)
	// Fetch is the unary cold-start or reconnect path.
	// When last_version and last_checksum both still match the current snapshot,
	// the server returns Unchanged. Otherwise it returns the current envelope.
	Fetch(context.Context, *connect.Request[v1.FetchRequest]) (*connect.Response[v1.FetchResponse], error)
}

// NewSnapshotServiceClient constructs a client for the orange.config.v1.SnapshotService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewSnapshotServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) SnapshotServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	snapshotServiceMethods := v1.File_orange_config_v1_service_proto.Services().ByName("SnapshotService").Methods()
	return &snapshotServiceClient{
		watch: connect.NewClient[v1.WatchRequest, v1.WatchResponse](
			httpClient,
			baseURL+SnapshotServiceWatchProcedure,
			connect.WithSchema(snapshotServiceMethods.ByName("Watch")),
			connect.WithClientOptions(opts...),
		),
		fetch: connect.NewClient[v1.FetchRequest, v1.FetchResponse](
			httpClient,
			baseURL+SnapshotServiceFetchProcedure,
			connect.WithSchema(snapshotServiceMethods.ByName("Fetch")),
			connect.WithClientOptions(opts...),
		),
	}
}

// snapshotServiceClient implements SnapshotServiceClient.
type snapshotServiceClient struct {
	watch *connect.Client[v1.WatchRequest, v1.WatchResponse]
	fetch *connect.Client[v1.FetchRequest, v1.FetchResponse]
}

// Watch calls orange.config.v1.SnapshotService.Watch.
func (c *snapshotServiceClient) Watch(ctx context.Context, req *connect.Request[v1.WatchRequest]) (*connect.ServerStreamForClient[v1.WatchResponse], error) {
	return c.watch.CallServerStream(ctx, req)
}

// Fetch calls orange.config.v1.SnapshotService.Fetch.
func (c *snapshotServiceClient) Fetch(ctx context.Context, req *connect.Request[v1.FetchRequest]) (*connect.Response[v1.FetchResponse], error) {
	return c.fetch.CallUnary(ctx, req)
}

// SnapshotServiceHandler is an implementation of the orange.config.v1.SnapshotService service.
type SnapshotServiceHandler interface {
	// Watch is the steady-state live-update channel.
	// The client keeps the stream open; the server pushes a new SnapshotEnvelope
	// on every config change and emits a Heartbeat on the idle timer.
	Watch(context.Context, *connect.Request[v1.WatchRequest], *connect.ServerStream[v1.WatchResponse]) error
	// Fetch is the unary cold-start or reconnect path.
	// When last_version and last_checksum both still match the current snapshot,
	// the server returns Unchanged. Otherwise it returns the current envelope.
	Fetch(context.Context, *connect.Request[v1.FetchRequest]) (*connect.Response[v1.FetchResponse], error)
}

// NewSnapshotServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewSnapshotServiceHandler(svc SnapshotServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	snapshotServiceMethods := v1.File_orange_config_v1_service_proto.Services().ByName("SnapshotService").Methods()
	snapshotServiceWatchHandler := connect.NewServerStreamHandler(
		SnapshotServiceWatchProcedure,
		svc.Watch,
		connect.WithSchema(snapshotServiceMethods.ByName("Watch")),
		connect.WithHandlerOptions(opts...),
	)
	snapshotServiceFetchHandler := connect.NewUnaryHandler(
		SnapshotServiceFetchProcedure,
		svc.Fetch,
		connect.WithSchema(snapshotServiceMethods.ByName("Fetch")),
		connect.WithHandlerOptions(opts...),
	)
	return "/orange.config.v1.SnapshotService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case SnapshotServiceWatchProcedure:
			snapshotServiceWatchHandler.ServeHTTP(w, r)
		case SnapshotServiceFetchProcedure:
			snapshotServiceFetchHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedSnapshotServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedSnapshotServiceHandler struct{}

func (UnimplementedSnapshotServiceHandler) Watch(context.Context, *connect.Request[v1.WatchRequest], *connect.ServerStream[v1.WatchResponse]) error {
	return connect.NewError(connect.CodeUnimplemented, errors.New("orange.config.v1.SnapshotService.Watch is not implemented"))
}

func (UnimplementedSnapshotServiceHandler) Fetch(context.Context, *connect.Request[v1.FetchRequest]) (*connect.Response[v1.FetchResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("orange.config.v1.SnapshotService.Fetch is not implemented"))
}
