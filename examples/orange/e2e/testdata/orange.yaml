providers:
  github_models:
    kind: openai
    endpoint: https://models.inference.ai.azure.com
    path_prefix: ""
    auth:
      type: bearer
      secret_ref: env://GITHUB_TOKEN

models:
  gpt-4o-mini:
    provider: github_models
    metadata:
      tier: fast
