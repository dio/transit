admin:
  address:
    socket_address:
      address: 127.0.0.1
      port_value: {{ .AdminPort }}

static_resources:
  listeners:
  - name: mcp-profile-router
    address:
      socket_address:
        address: 127.0.0.1
        port_value: {{ .ProxyPort }}
    filter_chains:
    - filters:
      - name: envoy.filters.network.http_connection_manager
        typed_config:
          "@type": type.googleapis.com/envoy.extensions.filters.network.http_connection_manager.v3.HttpConnectionManager
          stat_prefix: mcp_profile_router
          http_filters:
          - name: mcp-profile-router
            typed_config:
              "@type": type.googleapis.com/envoy.extensions.filters.http.dynamic_modules.v3.DynamicModuleFilter
              dynamic_module_config:
                name: mcp-profile-router
              filter_name: mcp-profile-router
              filter_config:
                "@type": type.googleapis.com/google.protobuf.StringValue
                value: '{}'
          - name: envoy.filters.http.router
            typed_config:
              "@type": type.googleapis.com/envoy.extensions.filters.http.router.v3.Router
          route_config:
            name: local_route
            virtual_hosts:
            - name: local_service
              domains: ["*"]
              routes:
              - match:
                  prefix: "/_egress/github/mcp"
                route:
                  cluster: mcp-github
                  prefix_rewrite: "/mcp"
              - match:
                  prefix: "/_egress/kiwi/mcp"
                route:
                  cluster: mcp-kiwi
                  prefix_rewrite: "/mcp"
              - match:
                  prefix: "/"
                route:
                  cluster: mcp-blackhole

  clusters:
  - name: mcp-github
    type: STRICT_DNS
    connect_timeout: 1s
    load_assignment:
      cluster_name: mcp-github
      endpoints:
      - lb_endpoints:
        - endpoint:
            address:
              socket_address:
                address: {{ .GitHubHost }}
                port_value: {{ .GitHubPort }}
  - name: mcp-kiwi
    type: STRICT_DNS
    connect_timeout: 1s
    load_assignment:
      cluster_name: mcp-kiwi
      endpoints:
      - lb_endpoints:
        - endpoint:
            address:
              socket_address:
                address: {{ .KiwiHost }}
                port_value: {{ .KiwiPort }}
  - name: mcp-blackhole
    type: STATIC
    connect_timeout: 1s
    load_assignment:
      cluster_name: mcp-blackhole
      endpoints:
      - lb_endpoints:
        - endpoint:
            address:
              socket_address:
                address: 127.0.0.1
                port_value: 1
